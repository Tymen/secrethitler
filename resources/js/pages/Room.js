import React, {Component} from 'react';

export default class Lobby extends Component {
    state = {
        active: false
    }

    render() {
        if (this.s)
        return (
            <div>
                <h1>Room</h1>
            </div>
        );
    }
}
